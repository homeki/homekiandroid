<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <EditTextPreference
        android:key="server_url"
        android:summary="URL to the Homeki Server (e.g. 192.168.0.100)."
        android:title="Homeki Server URL" />
    <EditTextPreference
        android:key="server_port"
        android:summary="Port to the Homeki Server (e.g. 5000)."
        android:title="Homeki Server Port" />
    <CheckBoxPreference
        android:key="client_registering"
        android:summary="If this device should try to register as a client when connecting to wifi."
        android:title="Client Registering" />
	<Preference 
        android:key="set_home_ssid"
        android:onClick=""
        android:summary="Set the connected WLAN as the home WLAN."
        android:title="Set home WLAN" />

</PreferenceScreen>